#include <iostream>
#include <vector>
#include <map>
#include <unordered_set>
#include <bitset>
#include <cmath>

using namespace std;

int main() 
{
    int N;
    cin >> N;

  
    int result = static_cast<int>(std::sqrt(N));

   cout << result;

    return 0;
}